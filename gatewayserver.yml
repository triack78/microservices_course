server:
  port: 8072

#In questo modo il gateway server potrà avere le info su tutte le istanze attualmente attive
eureka:
  instance:
    preferIpAddress: true
  client:
    registerWithEureka: true
    fetchRegistry: true
    serviceUrl:
      defaultZone: "http://localhost:8070/eureka/"
